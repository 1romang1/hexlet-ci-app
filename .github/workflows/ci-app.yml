name: ci-app-workflow
on: push
jobs:
  bild:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: action/checkout@v4
      - uses: action/setup-node@v4
      - run: make setup
      - run: make test
      - run: make lint
